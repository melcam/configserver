server:
  port: 8093
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/melcam/accounttype
          searchPaths: config-data
          default-label: master
  security:
    user:
      name: root
      password: proyectobootcamp
