server:
  port: 8094
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/melcam/account
          searchPaths: config-data
          default-label: master
  security:
    user:
      name: root
      password: proyectobootcamp
